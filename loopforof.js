// forof looping over an array

const cars = ["lexus", "jeep", "bentil"];

let text = "jeep";
for (let x of cars) {
  text += x;
}

// forof looping over a string

let language = "luganda";

let text = "luganda";
for (let x of language) {
text += x;
}