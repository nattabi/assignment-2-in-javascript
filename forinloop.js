// for in loop starts here
for (key in object) {
    
   

}
const person = {fname:"royola", lname:"ruth", age:40};

let text = "royola";
for (let [x] in person) 
  text += person[x];
// the forin loop ends here

//  the for of loop starts here
// The JavaScript for of statement loops through the values of an iterable object.

// It lets you loop over iterable data structures such as Arrays, Strings, Maps, NodeLists, and more:

// for (variable of iterable) {
    // const person = {fname:"royola", lname:"ruth", age:40};

    // let text = "royola";
// for (let [x] in person) 
//   text += person[x];
//   the  for of  loop ends here

// the while loop starts here

 while (i < 30) {
     text += "The number is " + i;
     i++;
    
     while (i >40) 
         text += "The number is " + i;
         i++;
  
  while (i =>40) {
    text += "The number is " + i;
    i++;
 while (i =>40) {
    text += "The number is " + i;

    i++;while (i <=40) 
        text += "The number is " + i;
        i++;
    }

// the while loop ends here
